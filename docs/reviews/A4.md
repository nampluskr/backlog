# A4 — backlog Phase 4 적대적 검증 (통합 + README)

- 구현자: Claude Code
- 검토 모델: gpt-5.6-sol (Codex CLI 0.149.0)
- 대상: `README.md` (Phase 4 산출물). 신규 제품 소스 없음 — 문서 검증 모드
  (`ADVERSARIAL-REVIEW.md` §6, 읽기 전용 셸 허용)
- 관련 PLAN 조항: PLAN.md "Phase 4", SPEC.md §10(완료 조건), DECISIONS.md D-2·D-5·D-6
- 실행 회차:
  - 1/3 — 2026-08-28, 유효, Critical 0 / Major 2 / Minor 1 / 근거부족 1
  - 2/3 — 2026-08-28, 유효, 1-2·1-3·1-4 해소, 1-1 은 Minor로 잔존, 신규 Major 1 / Minor 1
  - 3/3 — 2026-08-28, 유효, 5·6 해소 확인, Critical 0 / Major 0 / Minor 1 (신규)

**Verification Attempt Limit (3회) 소진.** 미해결 Critical·Major 없음.
Phase 4는 필수 통과 Phase 아님 → 완료.

## Phase 4 자체 검증 증거

```
backlog --file backlog.json validate                                  -> VALID 16 task(s)  (exit 0)
backlog --file d:\projects\tools\youtube_downloader\backlog.json validate -> VALID 19 task(s)  (exit 0)
backlog --file d:\projects\tools\speech_transcriber\backlog.json validate -> VALID 19 task(s)  (exit 0)
python -m unittest discover -s tests                                  -> Ran 96 tests, OK
```

- 세 파일 모두 BOM 없음 (`7b ...` = `{`).
- 접두어: `BL-` / `YD-` / `ST-` 각 파일 단일. `add` probe로 `YD-064` 생성·재검증 통과 확인.
- 소비 워크스페이스 backlog 는 이 CLI 로만 관리 (D-5). `backlog/` 위치는 이미
  `d:\projects\tools\backlog` — DECISIONS "미확정"의 이동 여부 항목 해소.
- BL-001~BL-016 전부 `done` (BL-013 이후는 `backlog update` CLI 로 처리, dogfood).

## 지적

| # | 회차 | 심각도 | 요약 | 위치 | 처리 |
|---|---|---|---|---|---|
| 1 | 1 | Major | "파이썬 1:1 포팅" 이 의도된 설계 차이(접두어 감지, `ID_PREFIX_MIXED`, hook·MCP 제외)를 가림 | README:6 | 수정 — "다섯 명령을 파이썬으로 옮긴 것", 차이를 명시하고 D-2·D-6 참조 |
| 2 | 1 | Major | "Phase 4 완료" 주장 시점에 A4 미기록, "각 Phase" 표현이 Phase 0 포함해 부정확 | README:12-14 | 수정 — 이 문서(A4) 작성, "Phase 1~4", "Phase 0(스캐폴드)은 신규 소스 없어 제외" 명시 |
| 3 | 1 | Minor | 설치 전 실행 예제가 맨 `python -m backlog` 안내 (CLAUDE.md·SPEC §2 는 WinPython 절대경로 강제) | README:28 | 수정 — "위 인터프리터로 `-m backlog`", "맨 python 금지" 문구 추가 |
| 4 | 1 | 근거부족 | 세 외부 파일 validate 통과의 재현 근거 부재 | README:12 | 수정 — 이 문서에 실제 validate 출력(16/19/19) 기록 |
| 5 | 2 | Major | `pip install -e .` 후 "어느 디렉터리에서든 `backlog`" 주장 — WinPython `Scripts\`가 `PATH`에 없어 `backlog` 미발견 | README:30 | 수정 — `Scripts\` 절대경로 명시, "PATH에 있으면 …, 없으면 `...\Scripts\backlog.exe` 또는 `-m backlog`" |
| 6 | 2 | Minor | "데이터 계약과 종료 코드는 참조와 동일하나" 직후 계약 차이(ID 수용 범위, `ID_PREFIX_MIXED`) 나열 — 모순 | README:7 | 수정 — "task 구조와 종료 코드 체계는 대체로 참조를 따르되 아래 차이" |
| 7 | 3 | Minor | "Phase 0은 신규 소스가 없어" — 실제로는 `__init__`·`__main__`·`errors.py`·`cli.py` 골격 생성함 | README:18 | 수정 — "Phase 0(스캐폴드)은 별도 적대적 검증 기록이 없다" |

## 유효하지 않은 지적과 반박 근거

없음. 전 지적 수용.

## 미해결 · 남은 위험

- 없음. 3회 소진 시점 미해결 Critical·Major 0.
- 명령 문법·종료 코드·안전한 저장·`none`/`deps`/`done_at`/`meta.updated`·예제 실행
  가능성 모두 소스·명세와 일치 확인 (3/3 검토자 최종 확인).
- SPEC §2.1 도 맨 `python -m backlog` 표현을 쓰나 이는 SPEC 소관 — 필요 시 별도 정정.
- 테스트 96개, 세 backlog(16/19/19) validate 통과.
