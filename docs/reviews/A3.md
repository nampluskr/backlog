# A3 — backlog Phase 3 적대적 검증

- 구현자: Claude Code
- 검토 모델: gpt-5.6-sol (Codex CLI 0.149.0)
- 대상 파일: `src/backlog/mutate.py`, `src/backlog/cli.py`
  (`_print_validation_errors`, `_load_and_prevalidate`, `_postvalidate_and_write`,
  `cmd_add`, `cmd_update`)
- 관련 PLAN 조항: PLAN.md "Phase 3", SPEC.md §3·§4·§5·§6(안전한 저장)·§7(종료 코드),
  DECISIONS.md D-2·D-4·D-5
- 실행 회차:
  - 1/3 — 2026-08-28, 유효(프롬프트 일부 잘림, 검토는 유효), Critical 0 / High 1
  - 2/3 — 2026-08-28, 유효, High 1건 해소 확인, 신규 1건(P2)
  - 3/3 — 2026-08-28, 유효, **지적 없음** — 두 수정 확인, 안전한 저장 순서·종료 코드 매핑 일치

**필수 통과 Phase.** 3회 종료 시점 미해결 Critical 0 → Phase 4 진행 가능.

## 지적

| # | 회차 | 심각도 | 요약 | 재현 조건 | 위반 조항 | 처리 |
|---|---|---|---|---|---|---|
| 1 | 1 | High | `compute_next_id`가 `int()`로 무한 길이 숫자 접미어 변환 → 4300자리 초과 시 `AA-001` 오반환 또는 미포착 트레이스백 | `validate` 통과하는 4300/4301자리 숫자 id 문서에 `add` | PLAN Phase 3(다음 번호 = max+1), SPEC §7(트레이스백 금지) | 수정 — `int()` 제거, 십진 문자열 연산 `_normalize_digits`/`_decimal_key`/`_increment_decimal` |
| 2 | 2 | Major(P2) | `compute_create_fields`가 `REQUIRED_CREATE_FIELDS`를 강제하지 않음 (참조·PLAN은 이 함수가 검사한다고 명시) | 직접 호출자가 `compute_create_fields({"title": "x"})` → `build_task`에서 `KeyError` | PLAN Phase 3, 참조 `computeCreateFields`의 `MissingRequiredFieldsError` | 수정 — `MissingRequiredFieldsError` 추가, `compute_create_fields`에서 raise, `cmd_add`가 catch → 종료 코드 2 (파일 IO 전) |

## 유효하지 않은 지적과 반박 근거

없음.

## 미해결 · 남은 위험

- 없음 (3/3 지적 없음).
- 회귀 테스트 추가: `test_long_numeric_suffix_no_int_crash`, `ComputeCreateFieldsTest`,
  `test_add_to_empty_backlog_requires_prefix` (3/3 검토가 지적한 잔여 위험 — 빈 backlog +
  `--prefix` CLI 경로 — 를 통합 테스트로 커버).
- 검증 확인: 안전한 저장(read → parse → validate(pre) → deepcopy+mutate → validate(post) →
  임시파일 → `os.replace`), 실패 시 원본 byte 단위 불변, 종료 코드 2(인자)/1(데이터)/3(쓰기).
- 테스트 96개 전부 통과.
