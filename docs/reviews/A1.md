# A1 — backlog Phase 1 적대적 검증

- 구현자: Claude Code
- 검토 모델: gpt-5.6-sol (Codex CLI 0.149.0)
- 대상 파일: `src/backlog/document_io.py`, `src/backlog/validate.py`,
  `src/backlog/errors.py`, `src/backlog/cli.py` (`_load_document`, `cmd_validate`)
- 관련 PLAN 조항: PLAN.md "Phase 1", SPEC.md §5(validate)·§6(안전한 저장)·§7(종료 코드),
  DECISIONS.md D-2·D-4
- 실행 회차:
  - 1/3 — 2026-08-28, 유효, Critical 0 / Major 3 / Minor 0
  - 2/3 — 2026-08-28, 유효, 보완 확인 — 신규 결함 없음, 3건 모두 해소
  - 3/3 — 미사용

## 지적

| # | 심각도 | 요약 | 재현 조건 | 위반 조항 | 처리 |
|---|---|---|---|---|---|
| 1 | Major | `json.loads`가 `NaN`/`Infinity`/`-Infinity`를 수용, `json.dumps`가 이를 출력 | 문서에 `"probe": NaN` 등 추가 후 `validate` → `VALID` | SPEC §5(JSON 문법 검증), §6-6(직렬화), §7(JSON 오류 = 코드 1), D-4 | 수정 — `read_document`는 `parse_constant=_reject_constant`로 `DocumentParseError` 발생, `write_document`는 `json.dumps(allow_nan=False)` + `ValueError`→`FileWriteError` |
| 2 | Major | ID 정규식 `\d`가 비ASCII 십진 숫자(예: `BL-٠٠١`)를 통과 | task id를 `AA-٠٠١`로 두고 `validate` → 통과 | PLAN Phase 1(참조 `^[A-Z]{2,}-\d{3,}$` 계약), SPEC §4·§5, D-2 | 수정 — `DATE_RE`, `ID_RE`를 `re.ASCII`로 컴파일 |
| 3 | Major | 임시파일 쓰기 중 `UnicodeEncodeError`가 `FileWriteError`로 매핑되지 않고 임시파일도 정리 안 됨 | 문서에 lone surrogate(`title: "\ud800"`) 포함 후 `write_document` 호출 | PLAN Phase 1(실패 시 임시파일 정리), SPEC §6, §7(코드 3, 트레이스백 금지), D-4 | 수정 — `except (OSError, UnicodeError)`로 확장, 정리 후 `FileWriteError` |

## 유효하지 않은 지적과 반박 근거

없음. 3건 모두 유효로 인정하고 수정.

## 미해결 · 남은 위험

- 없음. 재검토(2/3)에서 3건 해소 및 신규 결함 없음 확인.
- 회귀 테스트 추가: `test_nan_infinity_rejected_as_parse_error`,
  `test_id_rejects_non_ascii_digits`, `test_unencodable_string_raises_write_error_and_cleans_temp`.
- Phase 1은 필수 통과 Phase이며 미해결 Critical이 없으므로 Phase 2 진행 가능.
